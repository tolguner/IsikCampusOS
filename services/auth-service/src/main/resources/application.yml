server:
  port: 8081
 
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: auth-service
 
  datasource:
    url: jdbc:postgresql://localhost:5433/auth_db
    username: campusos
    password: campusospassword
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      properties:
        max.block.ms: 2000
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:false}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:false}
            required: ${MAIL_SMTP_STARTTLS_REQUIRED:false}
 
app:
  mail:
    from: ${MAIL_FROM:no-reply@isikcampusos.local}
    enabled: ${MAIL_ENABLED:true}
 
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
 
logging:
  level:
    com.isik.kampusos.kimlik: DEBUG
 
security:
  jwt:
    # JWT secret environment variable'dan okunur — API Gateway ile AYNI değer olmalı.
    secret: ${JWT_SECRET:3cfa76ef14937c1c0ea519f8fc29b945279b945279b945279b945279b945279b}
    expiration: 86400000
